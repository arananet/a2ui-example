# =============================================================================
# Railway Deployment Configuration
# =============================================================================
# https://docs.railway.app/reference/config-as-code
#
# IMPORTANT: In the Railway dashboard, set the "Root Directory" of your
# service to "src" for the cleanest build. If you leave the root
# as the repo root, the startCommand below handles the path change.
# =============================================================================

[build]
builder = "nixpacks"

[deploy]
startCommand = "cd src && uvicorn main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
